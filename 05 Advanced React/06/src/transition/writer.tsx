import React from "react";

import { WriterContainer } from "./styled-elements";

const Writer = (): JSX.Element => {
  return (
    <React.Fragment>
      <WriterContainer>Codelicks Academy</WriterContainer>
    </React.Fragment>
  );
};

export default Writer;
